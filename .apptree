# ARBORESCENCE DES PAGES REACT - MAGIC STACK
# ============================================

localhost:5175/
│
├── / (HomePage - Accueil)
│   ├── [Game] → /game
│   ├── [Editor] → /editor  
│   ├── [Unified] → /unified
│   ├── [Start TCG] → /start-tcg
│   └── Accès rapides (Multiplayer, AI Battle, Spectator)
│
├── /game (GameView)
│   ├── Combat TCG (via /start-tcg + /combat)
│   ├── Exploration
│   └── Brouillard de guerre
│
├── /editor (EditorView)
│   │
│   ├── MapIconPlacerV2 (React)
│   │   └── Éditeur Space-Time (grille carrée, 6D)
│   │
│   └── Outils d'import/test
│
├── /unified (UnifiedMapSystem)
│   ├── HexGrid
│   ├── SquareGrid
│   └── Controls unifiés
│
├── /multiplayer (MultiplayerMode)
├── /ai-battle (AIBattleAutoplay)
├── /spectator (SpectatorGodMode)
├── /backstory-editor (HeroBackstoryEditor)
├── /combat (AIBattleWrapper – hybride HTML + React)
├── /start-tcg (écran de démarrage TCG)
├── /chasse-mega (Chasse Temporelle 6×6)
├── /settings, /about

# AUTRES PAGES HTML (servies sur port 8000)
# ==========================================

localhost:8000/
│
├── MAP_ICON_PLACER_ADVANCED.html (Original HTML adoré)
├── VINCENT_DASHBOARD_PRINCIPAL.html
├── API_EXPLORER_COMPLETE.html (aussi sur React)
└── Autres HTML legacy...

# SERVICES BACKEND
# =================

localhost:3001 → Rust (calculs 6D, Q*)
localhost:8082 → Java (CRUD, APIs)
localhost:7500 → Vector DB
localhost:7501 → LLM Clippy
localhost:9000 → MCP Server (bridge unifié)

# COMMANDES GO
# =============

./go          → Aide
./go start    → Lance tout
./go game     → Ouvre localhost:5175/game
./go editor   → Ouvre localhost:5175/editor
./go admin    → Ouvre localhost:5175/admin
./go api      → Ouvre API Explorer
./go mcp      → Lance MCP Server
./go status   → État des services
