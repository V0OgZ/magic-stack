CONTEXTE GLOBAL - HEROES OF TIME - JOUR 35 (11 AOÛT)

=== ALERTE CRITIQUE - FICHIERS HORS GIT ===

PROBLÈME DÉCOUVERT:
Des fichiers créés HORS du repo git dans /Volumes/HOT_DEV/Magic/ :
- apps/ (dupliqué du magic-stack/apps/)
- hot/ (version différente de magic-stack/hot/)
- clippy_*.py (plusieurs versions)
- LANCE_CLIPPY_MEMENTO.sh

STRUCTURE RÉELLE:
/Volumes/HOT_DEV/Magic/              ❌ PAS UN REPO GIT
└── magic-stack/                     ✅ LE VRAI REPO GIT

=== VISION DU JEU (Vincent) ===

Heroes of Time = Jeu stratégique multivers avec CD Engine (Causalité-Déphasée)
- Inspiré Heroes of Might & Magic mais avec TEMPS comme terrain de jeu
- Brouillard de causalité remplace tour par tour
- Actions superposées jusqu'à observation (quantique)
- Combat TCG (cartes) en attendant animations complètes
- Sortie prévue 2026

=== ARCHITECTURE V2 (Specs complètes) ===

CONCEPTS CLÉS:
- Positions 6D: x,y,z,t (temps), c (causal), psi (quantum)  
- Énergie complexe: E = A + iΦ (amplitude + phase)
- Drift temporel: t_w (monde) vs t_e (entité)
- OPC (Ombre Portée Causalité): brut/qualité/effectif
- Régulateurs: Anna, Vince, Overload (anti-abus)

APIs V2 OFFICIELLES (v2spec/API_SURFACE_COMPLETE.md):

RUST (3001):
- /api/v2/tick, /api/v2/entity/{id}
- /agents/plan, /agents/fork, /agents/merge
- /api/map/generate, /api/map/init

JAVA (8082):
- /api/magic/cast, /api/magic/formulas (869!)
- /api/interstice/upload, /api/interstice/download
- /api/regulators/*

MANQUANT (montrés dans API_EXPLORER_COMPLETE.html mais N'EXISTENT PAS):
- /api/heroes ❌
- /api/creatures ❌
- /api/artifacts ❌
- /api/portals ❌

=== ÉTAT ACTUEL JOUR 35 ===

INFRASTRUCTURE:
- 1 seul serveur React (5175)
- Script ./go unifié (pas de go2!)
- Dashboard: http://localhost:5175/dashboard.html
- Backend Rust lancé sur 3001

=== MISES À JOUR 2025-08-12 ===
JAVA (8082):
- OpenAPI: /openapi.json (springdoc)
- Swagger UI: /docs
- Nouveau: endpoints Combat minimalistes
  - POST /api/combat/start
  - POST /api/combat/play-card
  - GET  /api/combat/state/{gameId}

FRONTEND (5175):
- Nouvelle page: /start-tcg (sélection mode/deck, appelle /api/combat/start)

OUTILS TESTS:
- Playwright E2E en place (tests/e2e), plus générateurs de specs via tools/site_map

PORTAILS AJOUTÉS:
- 🌀 Avalon (dimensional)
- 🌌 Poche (pocket_world)
- ⏳ Temporel (temporal)
Dans AssetsService.ts mais pas d'API backend

MIGRATION REACT:
- Magic Stack Unified (5175) = app principale
- /unified = éditeur principal (90%)
- /combat = wrapper HTML (IA_VS_IA_AUTOPLAY.html)
- /chasse-mega = wrapper HTML (CHASSE_TEMPORELLE_MEGA_MAP.html)

=== PROBLÈMES IDENTIFIÉS ===

1. Fichiers créés HORS du repo git (critique!)
2. API_EXPLORER_COMPLETE.html montre des endpoints qui n'existent pas
3. Confusion entre specs V1 et V2
4. Frontend avec données en dur au lieu du backend

=== SOLUTION VINCENT ===

"Frontend bête, backend intelligent"
- Les ressources viennent du backend
- hot/content/ comme source de données
- Backend vérifie les formules correctes
- Pas de logique dans le frontend

=== PRIORITÉS IMMÉDIATES ===

1. Décider quoi faire avec fichiers hors GIT
2. Implémenter /api/heroes, /api/creatures, /api/artifacts
3. Utiliser hot/content/ comme source
4. Corriger API_EXPLORER_COMPLETE.html
5. Frontend appelle vraies APIs V2

=== FICHIERS CLÉS ===

DOCUMENTATION:
- .map = architecture et statut
- .contexte = ce fichier
- v2spec/API_SURFACE_COMPLETE.md = vraies APIs V2

SCRIPTS:
- ./go = script unifié (status, game, admin, combat, stop)
- ./h = menu avec toutes les pages

FRONTEND:
- apps/magic-stack-unified/ = app React principale
- public/dashboard.html = dashboard central

=== NEXT STEPS ===

1. Nettoyer fichiers hors GIT (attendre décision Vincent)
2. Créer endpoints /api/heroes, /api/creatures dans Rust
3. Charger données depuis hot/content/
4. Tester avec ./go game
5. Vérifier que tout est jouable